// Using age and hasTicket, print "Entry allowed" if age is above 18 AND the person has a ticket (use &&).

const age = 23;

const hasTicket = true;

age>18 && hasTicket? console.log('Entry allowed'): console.log('Not allowed');