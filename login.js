// Build a simple login system — print "Login successful" only if both username and password are correct.

const correctusername = 'Abdul';
const correctpassword = "aBdul";

const username = 'Abdul' ; 
const password = 'aBdul';

correctpassword === password && correctusername === username ? console.log('Login successful'): console.log('Invalid username or pasword');